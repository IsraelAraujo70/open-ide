name: Release & Publish

on:
  push:
    branches:
      - master

jobs:
  release:
    name: Release & Publish to NPM
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN || github.token }}

      - name: Check if should release
        id: should_release
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)

          if [[ $COMMIT_MSG =~ \[skip\ ci\] ]]; then
            echo "should_release=false" >> $GITHUB_OUTPUT
            echo "reason=skip-ci" >> $GITHUB_OUTPUT
            exit 0
          fi

          if [[ $COMMIT_MSG =~ ^chore\(release\): ]]; then
            echo "should_release=false" >> $GITHUB_OUTPUT
            echo "reason=release-commit" >> $GITHUB_OUTPUT
            exit 0
          fi

          if [[ $COMMIT_MSG =~ ^feat(\(.*\))?!: ]] || \
             [[ $COMMIT_MSG =~ ^feat(\(.*\))?: ]] || \
             [[ $COMMIT_MSG =~ ^fix(\(.*\))?: ]] || \
             [[ $COMMIT_MSG =~ ^BREAKING[[:space:]]CHANGE: ]] || \
             [[ $COMMIT_MSG =~ \[breaking\] ]]; then
            echo "should_release=true" >> $GITHUB_OUTPUT
            echo "✅ Commit triggers release"
          else
            echo "should_release=false" >> $GITHUB_OUTPUT
            echo "reason=non-release-commit" >> $GITHUB_OUTPUT
            echo "⏭️ Skipping release (commit type: ${COMMIT_MSG%%:*})"
          fi

      - name: Setup Node.js
        if: steps.should_release.outputs.should_release == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - name: Setup Bun
        if: steps.should_release.outputs.should_release == 'true'
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        if: steps.should_release.outputs.should_release == 'true'
        run: bun install --frozen-lockfile

      - name: Run tests
        if: steps.should_release.outputs.should_release == 'true'
        run: bun test

      - name: Type check
        if: steps.should_release.outputs.should_release == 'true'
        run: bunx tsc --noEmit

      - name: Configure Git
        if: steps.should_release.outputs.should_release == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Determine version bump
        if: steps.should_release.outputs.should_release == 'true'
        id: version
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          CURRENT_VERSION=$(node -p "require('./package.json').version")

          if [[ $COMMIT_MSG =~ ^feat(\(.*\))?!:|^BREAKING[[:space:]]CHANGE: ]] || [[ $COMMIT_MSG =~ \[breaking\] ]]; then
            VERSION_TYPE="major"
          elif [[ $COMMIT_MSG =~ ^feat(\(.*\))?: ]]; then
            VERSION_TYPE="minor"
          elif [[ $COMMIT_MSG =~ ^fix(\(.*\))?: ]]; then
            VERSION_TYPE="patch"
          else
            VERSION_TYPE="patch"
          fi

          echo "version_type=$VERSION_TYPE" >> $GITHUB_OUTPUT
          echo "current_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          echo "🔼 Bumping $VERSION_TYPE version from $CURRENT_VERSION"

      - name: Bump version
        if: steps.should_release.outputs.should_release == 'true'
        id: bump
        run: |
          VERSION_TYPE=${{ steps.version.outputs.version_type }}
          npm version $VERSION_TYPE -m "chore(release): bump version to %s [skip ci]"
          NEW_VERSION=$(node -p "require('./package.json').version")
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "✨ New version: $NEW_VERSION"

      - name: Push changes and tags
        if: steps.should_release.outputs.should_release == 'true'
        run: git push origin master --follow-tags

      - name: Create GitHub Release
        if: steps.should_release.outputs.should_release == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.bump.outputs.new_version }}
          name: Release v${{ steps.bump.outputs.new_version }}
          body: |
            ## Release v${{ steps.bump.outputs.new_version }}

            ### Installation
            ```bash
            npm install -g @israelaraujo70/open-ide@${{ steps.bump.outputs.new_version }}
            ```

            ### Previous Version
            v${{ steps.version.outputs.current_version }}
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to NPM
        if: steps.should_release.outputs.should_release == 'true'
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Notify success
        if: steps.should_release.outputs.should_release == 'true' && success()
        run: |
          echo "✅ Successfully released v${{ steps.bump.outputs.new_version }} to NPM!"
          echo "📦 Package: https://www.npmjs.com/package/@israelaraujo70/open-ide"
          echo "🔖 GitHub Release: https://github.com/${{ github.repository }}/releases/tag/v${{ steps.bump.outputs.new_version }}"

      - name: Notify skip
        if: steps.should_release.outputs.should_release != 'true'
        run: |
          REASON=${{ steps.should_release.outputs.reason }}
          echo "⏭️ Release skipped"
          echo "Reason: $REASON"
          if [ "$REASON" = "skip-ci" ]; then
            echo "Commit contains [skip ci]"
          elif [ "$REASON" = "release-commit" ]; then
            echo "Commit is a release commit"
          else
            echo "Commit type does not trigger releases (only feat:, fix:, or breaking changes)"
          fi

      - name: Notify failure
        if: steps.should_release.outputs.should_release == 'true' && failure()
        run: |
          echo "❌ Release failed!"
          echo "Check the logs above for details."
